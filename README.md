# DESCRIPTION

Boton contador, una web diseñada para contabilizar cuantas veces se apieta o presiona un boton y se muestre la actualizacion por click, ademas cada 10 veces que presionas el boton el boton cambie de color

# Rules
Use in this case:
- Functions, arrays, condicionals structures, addEventListener...

# Logic

/*La logica es la siguiente:
Primero vamo a tomar el evento hacer click en el button
luego al hacerc lick aumetnamos el contaodr en 1
luego actualizar el display para que muestre el enumero de clicks
esto ultimo se hace concatenando strings html */

# Elementos utilizados
- Innertext: Va a establecer el contenido textual visible en un documento HTML.
- Math.random(arguments): Se encarga de generar numeros aleatorios entre 0 y 1
- Math.floor(arguments): Redondea el numero al entero mas cercano
